export type IHeaderNavItem = {
    text: string;
    link: string;
}

export type IheaderNavCta = {
    text: string;
    hoverText: string;
    background: string;
    link: string;
    iconType: string;
}